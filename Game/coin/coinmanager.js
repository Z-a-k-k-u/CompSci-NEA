function generateCoinPattern(number, platx, play){
  let coinArray = new Array(1).fill(new Array[1])
  switch(number){
    case 1:
      for(c of CP1){
        switch(c){
          case 1:
            let coinx = plax
            let coiny = platy + 100
            coinArray .push(new Coin())
        }
      }
  }
}
